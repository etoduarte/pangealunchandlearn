## Code Quality Rules

- CSS custom properties for ALL brand tokens — no hardcoded colors.
- Semantic HTML sections for each slide.
- Mobile-responsive by default.
- Animations via CSS where possible, JS only when needed.
